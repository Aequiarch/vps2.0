name: VPS SSH Session

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Tmate session
        uses: mxschmitt/action-tmate@v3
